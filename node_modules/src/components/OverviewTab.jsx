import EnhancedDashboard from './EnhancedDashboard';

function OverviewTab({ statistics, onDataUpdate }) {
  return <EnhancedDashboard statistics={statistics} onDataUpdate={onDataUpdate} />;
}

export default OverviewTab;
